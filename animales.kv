#:kivy 2.1.0
#:import random random
#:import os os

<AnimalMaster@HoverBehavior+Image>:
    on_enter: self.parent.callback()
    size: 140, 140
    anim_delay: 1/17
    pos: random.randint(50, 950), random.randint(50, 450)

<Animal>:
    AnimalMaster:
        source: root.fuente
        id: animal