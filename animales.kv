#:kivy 2.1.0
#:import random random
#:import os os

<AnimalMaster@ButtonBehavior+Image>:
    on_press: self.parent.callback()
    size: 200, 200
    anim_delay: 1/20
    pos: random.randint(50, 950), random.randint(50, 450)

<Animal>:
    AnimalMaster:
        source: 'video/'+random.choice(os.listdir("video"))